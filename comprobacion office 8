hostname

$rutas = @(
    "$env:ProgramFiles\Microsoft Office\Office16\vnextdiag.ps1",
    "${env:ProgramFiles(x86)}\Microsoft Office\Office16\vnextdiag.ps1"
)

$diagnostico = $rutas |
    Where-Object { Test-Path -LiteralPath $_ } |
    Select-Object -First 1

if ($diagnostico) {
    & $diagnostico -action list
}
else {
    Write-Host "No se encuentra vnextdiag.ps1 en las rutas habituales."
}




Get-ChildItem -Path "$env:ProgramFiles\Microsoft Office","${env:ProgramFiles(x86)}\Microsoft Office" -Filter vnextdiag.ps1 -Recurse -ErrorAction SilentlyContinue | Select-Object -ExpandProperty FullName



& "C:\Program Files\Microsoft Office\root\Office16\vNextDiag.ps1" -action list
