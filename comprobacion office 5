Get-WinEvent -FilterHashtable @{
    LogName = 'Microsoft-Windows-AAD/Operational'
    StartTime = (Get-Date).AddMinutes(-10)
} -MaxEvents 15 -ErrorAction Continue |
    Format-List TimeCreated, Id, LevelDisplayName, Message
